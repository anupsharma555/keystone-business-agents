# ANU-174 Linear Task Evidence

Updated: 2026-07-11

This is the authoritative evidence map for the 20 numbered tasks in Linear
issue ANU-174. Those rows are not the same cases as this repository's
`SMK-01` through `SMK-20`; neither list may be used as a proxy for the other.

Status means:

- **PASS:** direct current evidence covers route, useful output,
  source/provider proof, and the safety boundary.
- **PARTIAL:** meaningful framework, provider, or model evidence exists, but
  at least one required step in the exact Linear task remains unproved.
- **PENDING:** no current evidence is strong enough to credit the task beyond
  adjacent framework capability.

## Current Scorecard

| ID | Linear operator task | Status | Current evidence | Smallest remaining proof |
|---|---|---|---|---|
| L174-01 | Find the latest Gmail with an event or meeting time and add it to Google Calendar. | PASS | A bounded local Gmail search inspected one event-like message, extracted one unique future July 24 date plus 23:59 time and two hashed participant identities, and persisted only message/thread/subject hashes. The zero-model joined runner created one exact marked event on the primary Calendar, preserved `America/New_York`, verified provider state, deleted it, and verified absence. No invitations, Gmail mutation, raw-body persistence, send, or OpenAI request occurred. Timed create/update/delete and multi-date ambiguity blocking now have focused regression coverage. | Reuse this pass; rerun only after Gmail candidate selection, local event extraction, or timed Calendar CRUD changes. |
| L174-02 | Read the Airtable financial tracker and summarize the current quarter. | PASS | A natural Chief request resolved `current quarter` to the current year/quarter, read the live Finance & Tax Tracker schema plus schema-selected income/expense tables, and returned totals, table/field basis, expense categories, and an uncategorized-record count. The redacted receipt confirmed deterministic execution and no Airtable mutation; zero model requests were used. | Reuse this pass; rerun only after Airtable period, aggregation, or schema-selection behavior changes. |
| L174-03 | Summarize the most recently added Zotero article in one paragraph. | PASS | A refreshed live top-level Zotero read filtered `journalArticle`, sorted `dateAdded` descending, and again resolved exact item `9RFK7932`. DOI normalization removed the imported `/full` suffix and Crossref returned seven structured fields including the abstract. One no-tool `gpt-5.4-mini` request then produced a source-faithful claim/method/practical-relevance summary, explicitly distinguished abstract metadata from full-text review, retained exact item/source identity, and requested no search, send, or provider write. Usage: 1 request, 31,381 input tokens, 1,062 output tokens; maintained estimate `$0.02831475`; billing remained `$3.20` immediately before and after. | Reuse this pass; rerun only if latest-item resolution, DOI enrichment, or the bounded research-brief synthesis contract changes. |
| L174-04 | Research NeuroFlow and provide a concise company summary in Google Docs. | PASS | Direct Trafilatura extraction of NeuroFlow's official HTTPS page produced ten claim candidates without broad search. One no-tool `gpt-5.4-mini` request returned a concise source-visible company brief covering product, customers, first-party traction claims, unknown leadership, and Keystone relevance. The exact persisted result then drove a zero-model Google Doc lifecycle: create/read, same-ID review-note update/read, exact trash, and provider trash read-back all passed. Usage was one request, 31,564 input and 635 output tokens, zero retries, with a maintained `$0.0265305` estimate; Chrome credit moved from `$2.64` to `$2.62`. A shared aggregation fix now prevents live website inputs from acquiring synthetic fixture source identities, and the joined runner checks both Workspace write gates before any model call. | Reuse this pass; rerun only if direct-source aggregation, focused-brief synthesis, or the research-to-Doc lifecycle contract changes. |
| L174-05 | Find recent funding news for Headway and assess KNI relevance. | PASS | The natural ask routes to Business Research with target `Headway`, live-search intent, and read-only policy. One bounded Exa request returned seven candidates; deterministic entity/funding filters retained five Headway mental-health sources and rejected unrelated Headway organizations. One no-tool `gpt-5.4-mini` request correctly identified the latest verified event as the July 23, 2024 $100M Series D at a reported $2.3B valuation, tied the planned Medicare Advantage/Medicaid and clinician-operations expansion to KNI's payer, behavioral-health, clinical-AI, and research-operations interests, and explicitly said the 718-day-old round is not recent or evidence of an immediate opportunity. Exa's crawl timestamp initially polluted freshness metadata; source-publication dates now take precedence, and deterministic normalization removes superseded metadata-conflict commentary. Usage: 1 request, 33,491 input and 1,473 output tokens, zero retries/tools, maintained estimate `$0.03174675`; Chrome remained `$2.62` before and after. No send, draft, or write occurred. | Reuse this pass; rerun only if funding-date normalization, source filtering, or the bounded relevance contract changes. |
| L174-06 | Read the most recent non-newsletter Gmail thread from the last 7 days and draft a three-sentence reply without sending. | PASS | A bounded selected-thread read plus `gpt-5.4-mini` used thread history, summarized intent, produced concise reply text, and kept `draft_created=false` with no send/write. | Reuse this pass; rerun only after relevant Gmail selection/drafting changes. |
| L174-07 | Triage the latest Gmail messages for high-priority KNI follow-ups. | PASS | The final bounded `gpt-5.4-mini` stability run assigned all fixture messages exactly once, preserved sensible priority/bucket alignment and actions, and made no draft/write/send. | A larger real-inbox sample is optional breadth, not required to retain the basic capability pass. |
| L174-08 | Create an Airtable write plan for the latest available business-expense receipt. | PASS | Receipt extraction/mapping, live Airtable schema typing, review-only planning, and no-unapproved-write gates pass; the typed provider attachment lifecycle is separately proven. | Reuse this plan-level pass; natural attachment execution remains tracked under ANU-198. |
| L174-09 | Summarize the latest Slack thread in `ai-agents-workflow` and identify action items. | PASS | Connected Slack search resolved the newest actual replied thread, a July 10 Chief Calendar note-modification request, without expanding KBA's bounded reply probes. KBA then read that exact two-message thread through its own Slack tool and produced a zero-model summary with hashed thread identity, the provider-verified update decision, and the correct future action: identify the event by title and add the date only when disambiguation is needed. Shared compaction now removes Slack's `Sent using` footer, retains Calendar provider IDs only internally, and no longer misclassifies hyphen-separated receipt metadata as action items. Zero posts, mutations, or OpenAI requests occurred. | Reuse this pass; rerun only after Slack thread discovery, receipt compaction, or action extraction changes. |
| L174-10 | Inspect the latest `@KNI` Slack request, preserve/create its WorkItem, and report the selected route. | PASS | A bounded read selected the newest user-authored `@KNI` request from `ai-agents-workflow`, stored the exact request in a local WorkItem, and returned hashed Slack reference, WorkItem ID/state, Business Research route, and Opportunity Scout review next action. The receipt confirms zero OpenAI requests, live search, Slack posts, sends, or external writes. | Reuse this pass; rerun only after latest-request selection, WorkItem creation, or deterministic routing changes. |
| L174-11 | Compare three behavioral-health companies in two paragraphs. | PASS | Direct no-search extraction succeeded for the official NeuroFlow, Headway, and Spring Health homepages. One no-tool `gpt-5.4-mini` request returned exactly two paragraphs: a shared platform/access/analytics pattern first, then distinct positioning for NeuroFlow's risk infrastructure, Headway's insurance-enabled provider network, and Spring Health's employer-focused connected care. It included all three official URLs and repeatedly marked the evidence as first-party claims without independent validation. Usage: 1 request, 32,257 input tokens including 25,344 cached, 1,016 output tokens; maintained estimate `$0.01165755`; billing moved from `$3.20` to `$3.18`. No search, retry, send, post, or provider write occurred. | Reuse this pass; rerun only if the source packets or multi-target output contract materially change. |
| L174-12 | Find a public business-development contact for NeuroFlow without drafting outreach. | PASS | The exact natural ask now routes to Business Research with `contact_discovery`, `contact_candidates`, target `NeuroFlow`, and read-only/no-draft policy. A bounded provider run read NeuroFlow's official About and Contact pages and used one Exa semantic-deepening request. It selected Michael Mucha, Chief Commercial Growth Officer, from first-party leadership markup and corroborated the current role with his public LinkedIn profile. The receipt records 0.98 confidence, why the commercial role is relevant, the official `info@neuroflow.com`/contact path, five Exa results, one corroborated candidate, zero OpenAI requests, no inferred personal email, no raw source content, no draft, and no send/write. | Reuse this pass; rerun only if contact routing, official leadership parsing, Exa corroboration, or the selected role materially changes. |
| L174-13 | Draft a 150-word source-backed NeuroFlow outreach email without sending. | PASS | Approved-context Outreach drafting, unsupported-claim validation, revision safety, and no-send/no-provider-draft gates pass with fixture/source evidence. | Optional copy-quality rerun only if the approved NeuroFlow source packet changes. |
| L174-14 | Search local KNI documents for the latest proposal or capability statement and summarize service areas. | PARTIAL | The exact natural ask selects guarded local KNI tools instead of hosted file search. KBA resolves 1,055 linked local documents into eight bounded matches and five read candidates with hashed path evidence, `local_only=true`, `send_enabled=false`, and zero writes. The one-request/$0.05 Chief lane now forces `store=false`, in-memory prompt caching, disabled tracing, no sensitive trace content, and no hosted files, web, MCP, background mode, tools, or writes. Its offline profile and 56 focused SDK/runner tests pass. Codex managed execution still denied the reviewed private excerpts before process start; zero model requests or spend occurred. | The KBA/OpenAI data-handling contract is ready, but external Chief synthesis remains a Codex managed-launch blocker. Run the reviewed command from the trusted operator runtime, use a local model, or enable an approved MAM/ZDR project boundary; do not disguise the payload or route around the denial. |
| L174-15 | Read any Google Doc in KNIOps and summarize key information. | PASS | One bounded live run searched `KNIOps` for `README.doc`, selected the unique provider identity, read all 1,096 characters with `truncated=false`, and summarized the workspace purpose, folder roles, Slack-to-Drive operating model, and safety boundaries. Offline semantic revalidation accepted the unchanged source-faithful output. The run used exactly three `gpt-5.4-mini` requests and two read-only provider operations; no retry, search, write, share, move, trash, Gmail draft, send, or Slack post occurred. | Reuse this pass; rerun only if selected-file identity, Workspace read tools, or the requested-dimension contract materially changes. |
| L174-16 | Turn the latest KNI research note in KNIOps into a one-page Google Doc brief. | PARTIAL | Live provider preparation now resolves one exact `KNIOps/Research` folder and its unique latest Google Doc, then reads the full 533-character body with `truncated=false`. The reusable resolver blocks ambiguous folders or tied latest timestamps and persists only identity/title/content hashes, counts, modified time, `send_enabled=false`, zero OpenAI requests, and zero writes. Pasted-note outlining and Workspace artifact gates pass separately. | One bounded model synthesis over this exact selected document must preserve source identity/citations and produce a one-page brief, followed by exact approved disposable Doc create/read-back/cleanup. |
| L174-17 | Search recent clinical-AI psychiatry preprints and summarize the top three. | PASS | Natural Preprints execution returned three unique current records with stable identities, URLs, relevance ranking, preliminary-evidence caveats, manager review, zero OpenAI requests, and no mutation/post. | Reuse this pass; model synthesis is optional only if it adds material judgment. |
| L174-18 | Use the latest Zotero article before Business Research on NeuroFlow and explain what changed. | PASS | Exact item `zotero:item:9RFK7932` and its normalized-DOI Crossref packet first passed through LangGraph before Business Research with exact identity and `provider_source_handoff`. A refreshed no-search official NeuroFlow homepage read then supplied current first-party company context. One no-tool `gpt-5.4-mini` request correctly kept the company/customer/product takeaways unchanged, added only an adjacent interoperability/data-pipeline lens, and explicitly rejected the FHIR–OMOP abstract as evidence about NeuroFlow itself. Usage: 1 request, 31,527 input tokens, 1,402 output tokens; maintained estimate `$0.02995425`; billing remained `$3.20` immediately before and after. No search, retry, send, post, or provider write occurred. | Reuse this pass; repeat only if the provider handoff, relevance-boundary contract, or source packets materially change. |
| L174-19 | Run Chief coordination over quarterly finance context before choosing the next business-development priority. | PARTIAL | A joined zero-model/provider run calculated the real current-quarter Airtable summary, preserved it as concrete graph evidence, ran Chief before Airtable staging and Airtable before Opportunity Scout, retained both supplied BD choices, created two opportunity artifacts, and ended `done` with no blocker or write. Fake-model control supplied Chief's specialist choice. | One bounded live-model Chief decision over the same provider packet, with a finance-aware priority rationale and visible specialist receipts; no search or write. |
| L174-20 | Prepare a weekly ops packet from Slack, Gmail, relevant completed agent runs, and Calendar. | PARTIAL | The reusable **Chief Prior Week Packet** workflow has a synthesis-ready July 4–11 assembly with Slack/Gmail summaries, two explicitly relevant completed runs, and Calendar split into non-recurring focus areas plus compact recurring cadence. Offline packet/runner gates prove one Chief turn, zero attached tools/search/writes, a `$0.05` ceiling, human-only metadata, exact `KNIOps` destination, and `#ops-finance` delivery intent. The external-synthesis projection retains only non-personal business summaries, day-level dates, actions, relevance, and aggregate usage while excluding provider identities, exact timestamps, people/owner labels, links, routes, WorkItem IDs, and receipt references. The managed reviewer nevertheless denied this safer launch before process start because internal business-operational summaries remain private; no API request or spend occurred. | Run the guarded one-request command directly in an appropriately approved operator terminal, then inspect its usage/cost/quality receipt. After a synthesis pass, separately render `KNI Weekly Operations Packet — YYYY-MM-DD` into one exactly resolved `KNIOps` Drive folder, verify it by read-back, and post a concise summary plus verified Doc link in `#ops-finance`. |

## Current Totals

- PASS: **16**
- PARTIAL: **4**
- PENDING: **0**
- Total: **20**

## Execution Order

Do not run these as a broad eval batch. All previously pending rows now have
direct evidence. Remaining partials are L174-14, 16, 19, and 20; keep the
private-context synthesis rows off the Codex-launch critical path and do not
start another OpenAI run without a new explicit allowance.

Promptfoo remains deferred. A task changes to PASS only when its exact evidence
row is updated; adjacent route, tool, or provider success is insufficient.
